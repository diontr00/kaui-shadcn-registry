# Your GitHub repository URL — shown in the site header
GITHUB_URL="https://github.com/your-username/kaui-shadcn-registry"

# The public URL where the site is deployed.
# Used to generate absolute URLs in registry JSON (registryDependencies).
# Use http://localhost:4321 for local dev; set to your real domain in production.
URL="http://localhost:4321"
