## What

<!-- One sentence: what does this PR do? -->

## Why

<!-- Why is this change needed? Link an issue if applicable. -->

## Checklist

- [ ] `pnpm check` passes locally
- [ ] `pnpm run build` passes locally
- [ ] New component: `registry.json` updated with deps and target
- [ ] New component: example file(s) added to `src/registry/examples/`
- [ ] New component: docs page added to `src/content/docs/components/`
- [ ] New component: types updated in `src/data/types.ts` and `src/data/globals.ts`
